<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<title>Генератор заявок клининга</title>
<style>
body {
    font-family: Arial, sans-serif;
    background: #f5f6f8;
    padding: 20px;
}
.container {
    max-width: 900px;
    margin: auto;
    background: #fff;
    padding: 20px;
    border-radius: 10px;
}
h2 {
    border-bottom: 2px solid #eee;
    padding-bottom: 5px;
}
label {
    display: block;
    margin-top: 10px;
}
input, select, textarea {
    width: 100%;
    padding: 8px;
    margin-top: 5px;
}
.inline {
    display: flex;
    gap: 10px;
}
.inline input, .inline select {
    flex: 1;
}
.checkbox-group label {
    display: inline-block;
    margin-right: 15px;
}
button {
    margin-top: 15px;
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
}
.result {
    white-space: pre-wrap;
    background: #f1f1f1;
    padding: 15px;
    margin-top: 20px;
    border-radius: 8px;
}
</style>
</head>

<body>
<div class="container">
<h1>🧹 Генератор заявки для клининга</h1>

<h2>Общая информация</h2>
<label>Вид уборки</label>
<select id="type">
    <option>Поддерживающая</option>
    <option>Генеральная</option>
    <option>После ремонта</option>
    <option>Офис</option>
</select>

<div class="inline">
    <div>
        <label>Дата и время</label>
        <input id="datetime">
    </div>
    <div>
        <label>Площадь (м²)</label>
        <input id="area">
    </div>
</div>

<label>Город</label>
<input id="city">

<label>Адрес</label>
<input id="address">

<h2>Заказчик</h2>
<label>Имя</label>
<input id="client">

<label>Телефон</label>
<input id="phone">

<h2>Финансы</h2>
<label>Общая сумма (₽)</label>
<input id="total" type="number" oninput="calcSalary()">

<label>Зарплата мастеру</label>
<select id="salaryType" onchange="calcSalary()">
    <option value="60">60 / 40</option>
    <option value="70">70 / 30</option>
    <option value="fixed">Фиксированная</option>
</select>

<label>Фиксированная сумма (если выбрано)</label>
<input id="fixedSalary" type="number" oninput="calcSalary()">

<p><b>Мастеру:</b> <span id="masterPay">0</span> ₽</p>
<p><b>Компании:</b> <span id="companyPay">0</span> ₽</p>

<h2>Работы</h2>

<label><input type="checkbox" id="windows"> Мытьё окон</label>
<div class="inline">
    <select id="windowsSide">
        <option>Одна сторона</option>
        <option>Две стороны</option>
    </select>
    <input id="windowsCount" placeholder="Количество створок">
    <select id="windowsType">
        <option>Стандартные</option>
        <option>Нестандартные</option>
    </select>
</div>

<label><input type="checkbox" id="dustCeiling"> Обеспыливание потолка</label>
<label><input type="checkbox" id="dustWalls"> Обеспыливание стен</label>

<label><input type="checkbox" id="closets"> Мытьё шкафов</label>
<select id="closetsType">
    <option>Снаружи</option>
    <option>Снаружи и внутри</option>
</select>
<select id="closetsStuff">
    <option>Без вещей</option>
    <option>Есть вещи</option>
</select>

<h3>Бытовая техника</h3>
<div class="checkbox-group">
<label><input type="checkbox" value="Духовой шкаф" class="tech"> Духовой шкаф</label>
<label><input type="checkbox" value="Плита" class="tech"> Плита</label>
<label><input type="checkbox" value="Холодильник" class="tech"> Холодильник</label>
<label><input type="checkbox" value="Вытяжка" class="tech"> Вытяжка</label>
<label><input type="checkbox" value="Микроволновка" class="tech"> Микроволновка</label>
<label><input type="checkbox" value="Чайник" class="tech"> Чайник</label>
<label><input type="checkbox" value="Кофемашина" class="tech"> Кофемашина</label>
<label><input type="checkbox" value="Посудомойка" class="tech"> Посудомойка</label>
</div>

<h3>Ванная</h3>
<label><input type="checkbox" id="tiles"> Кафель</label>
<label><input type="checkbox" id="plumbing"> Сантехника</label>
<label><input type="checkbox" id="shower"> Душевая</label>
<label><input type="checkbox" id="bath"> Ванна</label>

<h2>Дополнительно</h2>
<textarea id="comment" placeholder="Пожелания заказчика"></textarea>

<button onclick="generate()">Сформировать заявку</button>
<button onclick="copyText()">Скопировать</button>
<button onclick="sendTelegram()">Telegram</button>
<button onclick="sendWhatsApp()">WhatsApp</button>

<div id="output" class="result"></div>
</div>

<script>
function calcSalary() {
    const total = Number(document.getElementById('total').value || 0);
    const type = document.getElementById('salaryType').value;
    let master = 0;

    if (type === 'fixed') {
        master = Number(document.getElementById('fixedSalary').value || 0);
    } else {
        master = total * (type / 100);
    }

    document.getElementById('masterPay').innerText = master.toFixed(0);
    document.getElementById('companyPay').innerText = (total - master).toFixed(0);
}

function generate() {
    let tech = [...document.querySelectorAll('.tech:checked')].map(e => e.value).join(', ');

    let text = `
🧹 ЗАЯВКА НА УБОРКУ

Вид: ${type.value}
Дата и время: ${datetime.value}
Город: ${city.value}
Адрес: ${address.value}
Площадь: ${area.value} м²

Заказчик: ${client.value}
Телефон: ${phone.value}

💰 Финансы:
Общая сумма: ${total.value} ₽
Мастеру: ${masterPay.innerText} ₽

📋 Работы:
${windows.checked ? `Окна: ${windowsSide.value}, створок ${windowsCount.value}, ${windowsType.value}` : ''}
${dustCeiling.checked ? 'Обеспыливание потолка' : ''}
${dustWalls.checked ? 'Обеспыливание стен' : ''}
${closets.checked ? `Шкафы: ${closetsType.value}, ${closetsStuff.value}` : ''}
${tech ? 'Техника: ' + tech : ''}
${tiles.checked ? 'Кафель' : ''}
${plumbing.checked ? 'Сантехника' : ''}
${shower.checked ? 'Душевая' : ''}
${bath.checked ? 'Ванна' : ''}

Комментарий:
${comment.value}
`;
    output.innerText = text;
}

function copyText() {
    navigator.clipboard.writeText(output.innerText);
    alert('Скопировано');
}

function sendTelegram() {
    window.open(`https://t.me/share/url?text=${encodeURIComponent(output.innerText)}`);
}

function sendWhatsApp() {
    window.open(`https://wa.me/?text=${encodeURIComponent(output.innerText)}`);
}
</script>
</body>
</html>
